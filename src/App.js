export const luasSegitiga = (a,t) => {
    return (1/2*a*t);
};

export const luasLingkaran = (r)=>{
    return (3.15*r*r);
};

export const volKerucut = (r,t)=>{
    return (1/3*22/7*r*r*t);
}

export const luasLayang = (d1, d2)=>{
    return (1/2*d1*d2);
}

// export const add = (x,y) => x+y;